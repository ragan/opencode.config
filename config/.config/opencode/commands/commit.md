---
description: Auto-commit with generated message
---

Commit current changes to git. Follow these steps:

1. Run `git status` to see all untracked and modified files
2. Run `git diff HEAD` (staged and unstaged) to understand the changes
3. Analyze the changes and craft a concise commit message (1-2 sentences) following the repository's commit message style if documented in AGENTS.md
4. Create the commit with the generated message

$ARGUMENTS
